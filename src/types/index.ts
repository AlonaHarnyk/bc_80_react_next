export interface StudentItem {
    name: string;
    age: number;
    isOnline: boolean;
    id: number
}

export interface User {
    name: string;
    email: string;
    isOnline: boolean;
    id: string;
}